version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto21

  build:
    commands:
      - echo "Building application..."
      - mvn package -DskipTests

artifacts:
  files:
    - target/*.jar