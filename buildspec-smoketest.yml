version: 0.2

phases:
  build:
    commands:
      - set -x
      - echo "Waiting for app to start..."
      - sleep 30

      - echo "Running smoke tests..."
      - curl -v -f http://$PRODUCT_URL/actuator/health
      - curl -v -f http://$USER_ORDER_URL/actuator/health