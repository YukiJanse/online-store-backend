version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto21

  build:
    commands:
      - echo "Running tests..."
      - mvn clean verify

reports:
  unit-test-reports:
    files:
      - 'target/surefire-reports/*.xml'
    file-format: JUNITXML